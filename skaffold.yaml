apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/salaboy/sp1-gateway:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/salaboy/sp1-gateway:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: sp1-gateway
        chartPath: charts/sp1-gateway
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/salaboy/sp1-gateway"
          image.tag: "{{.TAG}}"
